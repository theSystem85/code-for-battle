// helpSystem.js
// Legacy help modal removed in favor of settings modal keybindings tab.
// This module remains as a compatibility stub for older imports.

export class HelpSystem {
  showControlsHelp() {
    // no-op
  }
}
