# 2026-04-19T20:42:23Z
Model: codex

Prompt:
Ensure in SSE the user can group tiles together to indicate that these tiles only can be used in combination with each other. For example can the user mark debris tiles on the sheet to get 1x2 or 2x1 or 2x2 or 3x3 and so on tiles. But ensure only rectangular shapes are supported. The user can select the "group" tag in the tags list to draw the group tag on the tiles of the current sprite sheet. on the tile each drawn cluster of group tags automatically gets a group number assigned. That number can also manually be set in the sidebar by a number input "group id" from 1 to 999. This input is located below the "add tag" input. when the user releases the mouse the number gets automatically increased by 1. the group label on each tile of a group must be "group_X" where X is the group number. Ensure this groups can be applied for now (in future for even more) for debris and decorative tiles and rocks. That means that the largest matching tile group will be chosen for rendering when it fits on the map. So for example when the map rendering is drawing rocks then the largest rock group will be drawn first on the map to fill the largest connected rock tile groups. Also apply this to decorative tiles. Debris will be drawn based on the footprint of the destroyed building so if the building was 3x3 then the debris group of 3x3 tiles will be searched in the sprite sheet. if none is found default 1x1 (no group) tiles will be used from the sheet.
