UTC: 2026-04-26T17:55:05Z
LLM: GPT-5.3-Codex

Prompt:
"ensure that in SSE when user drops a file on the canvas of the sprite sheet that both json files (used for tags) and webp or png files for sprite sheet assets get immediately loaded. Both was already implemented but when the json import by drop was implemented it prevents now that the images themselves can also still be loaded by dropping them on canvas (worked before). I get this error notification in the app when I try: \"Import failed: invalid JSON file\"). Keep in mind, when an images is dropped a new sprite sheet is created from it but when a json file is loaded it will define the tagging for the current sprite sheet."
