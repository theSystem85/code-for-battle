# UTC Timestamp
2026-04-15T20:05:35Z

# LLM
codex

# Prompt
when map settings get expanded in the sidebar ensure that the sidebar does not scroll to the very end of that expanded section but just so that the map settings are on top of the visible screen so the user can see the start of the expanded map settings.
