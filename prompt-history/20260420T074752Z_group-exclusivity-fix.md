UTC: 2026-04-20T07:47:52Z
Model: codex (GPT-5.3-Codex)

Prompt:
ensure that tiles marked with groups are only used in combination with that groups but not for smaller groups or non groups (single tiles). For example tiles marked with "debris" AND that are also in a group should not be used for displaying single non grouped debris for buildings that have 1x1 foot print. This should apply in general for all tiles marked with a group not just "debris" because that was just an example.
