# UTC Timestamp
2026-04-20T13:17:54Z

# LLM
codex

# Prompt
Now the tiles below the ore have no texture. Ensure they have the same random ground texture from the selected biome as the surrounding ones (same way it works when custom sprites are enabled; there is no such issue).
