UTC Timestamp: 2026-03-15T06:35:40Z
LLM: copilot

add a toggle checkmark in the settings modal for the new procedural water rendering so the user can switch between the classic water tile image based rendering and the new procedural one. Also add to the game settings modal a tone and saturation slider to controls the colors of the water rendering, the settings will be persisted in local storage like other settings do as well. Put in a new category into the runtime config for "Graphics" and put all these new settings there.