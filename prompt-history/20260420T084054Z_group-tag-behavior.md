UTC: 2026-04-20T08:40:54Z
Model: codex (GPT-5.3-Codex)

Prompt:
fix these issues:
1) if group selector is on and a tile in SSE is selected that already has a group tag then ensure to remove the group tag.

2) ensure for groups only to apply the "group_X" tag with X as a number BUT not the "group" tag without a number. Currently both tags are used for a single group but this is redundant.

3) ensure that single tiles cannot be grouped only when user clicks and drags a group can be applied to at least 2 tiles at once.
