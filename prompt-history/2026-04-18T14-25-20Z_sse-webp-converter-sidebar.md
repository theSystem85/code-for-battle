# Prompt History Entry
- UTC: 2026-04-18T14:25:20Z
- LLM: codex

## Prompt
ensure the SSE gets a section in the sidebar where the user can upload (local browser only) by drag and drop images that will immediately be converted to webp with by default 90% compression and a default resolution of 1024x1024 pixels. Ensure there is also an input each for compression and resolution so 3 number inputs in total. input resolution of the image is max 2048x2048 and jpeg and png are accepted as well as webp itself. The converted image will be "downloaded" immediately. The filename from the source will be copied over. Add a checkmark to let user decide if resolution and compression suffixes separated with "_" should be added to the filename. Put this entire conversion feature into a expand/collapse section at the bottom of the sidebar.
