# UTC Timestamp
2026-04-20T11:41:16Z

# LLM
codex

# Prompt
Ensure in SSE the user can also drop a json file containing tags for the current sprite sheet so it will be applied to the current sprite sheet and the tags are instantly updated with the tag data from that loaded sprite sheet json. The import format is the same as the format that is generated when "apply tags" button is pressed. Also add an "reset all" tags button to reset all current tags from the current sprite sheet.
