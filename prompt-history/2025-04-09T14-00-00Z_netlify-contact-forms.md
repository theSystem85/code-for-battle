# 2025-04-09T14:00:00Z
## LLM: copilot (Claude Opus 4.6)

Build German and English contact form pages using Netlify Forms (`data-netlify="true"`) for the serverless static site. No AJAX — plain HTML form submission with custom success pages. Also add `vite.config.js` multi-page build so all root HTML pages (legal, contact, success) are included in the `vite build` dist output.
