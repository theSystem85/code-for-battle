UTC: 2026-03-25T21:25:00Z
LLM: GPT-5.3-Codex

Prompt:
Add mobile support for guard mode. ensure if a friendly combat unit is selected and user taps on another friendly unit the guard mode gets active so that the selected unit protects the tapped unit. Ensure the guard icon is shown above the tapped/targeted unit when the combat unit is selected as long as the guard mode is active. The guard mode gets disabled when the combat units gets another order (like move, attack, stop). the combat unit can also guard multiple friendly units when dragging an AGF box around multiple friendly units. IF any enemy units are in the box these are attacked first while the guard mode for all friendly units gets active. This works the same in mobile as on desktop. So ensure on desktop there is no more need for holding the cmd key to use the guard mode on another unit. Just ensure that when hovering over a friendly unit while an own combat unit is already selected that the cursor turns into a guard cursor.
