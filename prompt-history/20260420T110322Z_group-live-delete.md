UTC: 2026-04-20T11:03:22Z
Model: codex (GPT-5.3-Codex)

Prompt:
tackle these issues:
1) ensure when group is being drawn that the group tags are immediately visible (when drawn over more than 1 tile) and have a yellow overlay.

2) ensure that when one group tile of an already existing group is removed the entire group gets removed as well

3) when rendering the map background tiles (like grass or soil and so on) then ensure that also bigger decorative tiles than 1x1 are applied when grouped decorative tiles are available in any sprite sheet that is enabled like 2x2 or 3x3 or others.
