# 2026-07-27T19:05:03Z

Processed by: Codex

## Prompt

1) remove battleship's independent turret HUD (green dashed circle around each turret and the red angle). It does not add any value anymore. Ensure to update the requirements as well.
2) ensure when any unit(s) are selected and user clicks on another unit that the selected units can interact with (like guard, get loaded into, get service from, ...) that only one action is applied to that units depending on this hierarchy of actions (first ones gets applied when possible):
[get loaded into, get service from, guard that unit] ONLY when none of these actions is applicable then the unit will be selected!
3) ensure all landed aircraft on a carrier is always rendered on top of the carrier (currently there is a bug and F22 cannot be seen anymore after landed).
