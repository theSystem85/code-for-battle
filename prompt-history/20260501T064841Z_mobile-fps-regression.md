UTC: 2026-05-01T06:48:41Z
LLM: codex

## Prompt
look at the latest specs and promt history to understand what I wanted to achieve on this branch. Here is also the old prompt: ```Since the last few commits regarding sprite sheet changes the mobile framerate dropped from 60fps to below 10. Check why and fix the issue(s). Then put some bottleneck performance monitor into the fps overlay on whether it is cpu or gpu or memory(bandwidth) or whatever so I see what the current limiting factor is in realtime.``` Since your last commit the issue is still far from being fixed, I actually saw now improvement on mobile at all. Dive deeper into the analysis and compare what was differernt when running the current deployed version https://deploy-preview-650--code-for-battle.netlify.app/ vs this older version https://deploy-preview-640--code-for-battle.netlify.app/. The versions diff should contain changes to how sprite sheets are handled (use one larger sheet instead of many smaller ones) and also SOT rendering changes. The issue might live there but do not rule out any other causes that you find on your own! Fix the most likely issues you find and make a list of issues you would also consider to fix but did not have the time to fix yet BUT fix as much as you can now! Give me a deep explanation on what you fixed and why in the end! Before you start to look how to fix the issue make an e2e performance benchmark that actaully replicates the issue of poor mobile performance (below 10fps) vs normal destop performance (above 60fps). Only when the benchmark replicates the issue start to analyse the code and try to fix it! Do not ask questions but upfront.
