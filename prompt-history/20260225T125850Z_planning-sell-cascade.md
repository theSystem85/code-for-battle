UTC: 20260225T125850Z
LLM: GPT-5.2-Codex

## Prompt

ensure buildings in planning mode also enables other planned buildings to be planned near (normal build distance) existing planning footprints even when they are not yet build. Ensure to show the build order number on every planning footprint. Ensure user can use the sell button to cancel any plan, then the order number need to be adjusted. Ensure existing plans when cancelled can lead to also cancelling consecutive plans when they are not in proximity to other base buildings or plans that still have connection to the base within build distance! If multiple plans would be cancelled the user has to click on that plan twice while having "sell" mode active. At the first click the user will see a notification with a warning that x building plans would also be cancelled (show warning with a warning icon after notification text). Also show the affected plan overlays on the map then in orange instead of blue until user cancels sell mode
