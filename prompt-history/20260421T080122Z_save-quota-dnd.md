# Prompt History
- UTC: 2026-04-21T08:01:22Z
- Model: codex

## Prompt
Ensure:
1) exported save game files can be dragged onto the game map to load that game. 
2) when user hovers over the savegame in the sidebar list show a tooltip with the filesize of the savegame next to how many MB are still left in the local storage before quota exceeds.
3) when local storage quota is exceeded and user tries to save a game that is too large catch the browser error, disable the savegame button (until quota gets freed up again by removing save games) and show a tooltip over the savegame button regarding exceeded quota and display also a notification that explains user how to export and load save games using files and drag n drop. 
4)Next to the current save button (that saves to local storage) also offer a direct download button that exports the save game and bypasses the local storage save.
