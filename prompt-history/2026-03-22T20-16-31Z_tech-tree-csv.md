# Prompt History

- Timestamp: 2026-03-22T20-16-31Z
- LLM: GitHub Copilot (GPT-5.4)
- Request: Make sure the LLM knows it can sell buildings to recover economy and always receives the full tech tree in a compressed CSV-like format.
- Changes:
  - Added `buildCompactTechTreeCsv()` in `src/ai-api/techTree.js` and exposed it as `techTreeCsv` inside the compact strategic digest.
  - Kept `productionOptions` for immediate legal actions while giving the LLM a separate compressed full-tech-tree string for long-term planning.
  - Strengthened the strategic prompt so selling lower-priority buildings to rebuild refinery/harvester economy is explicitly treated as correct behavior.
  - Added digest coverage in `tests/unit/llmStrategicDigest.test.js` for the new compressed tech-tree field.
- Validation:
  - `tests/unit/llmStrategicDigest.test.js`