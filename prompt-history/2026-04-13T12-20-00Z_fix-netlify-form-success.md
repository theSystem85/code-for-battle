# 2026-04-13T12:20:00Z
## LLM: copilot

Investigate why the deployed Netlify preview returns a 404 on `/kontakt-erfolg` after submitting the contact form even though the page exists on direct GET. Add the smallest code-side hardening needed so the form submission includes the expected Netlify `form-name` payload explicitly.