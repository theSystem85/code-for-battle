2026-09-26T01:08:32Z
Grok 4.7, Cursor Cloud Agent. Token counts and duration are not available for this turn.

User tested PR #712 on a real iPhone: Safari is now fine, but Chrome on iOS (CriOS) STILL shows the black band under the build bar on first portrait load (fixed only after rotating). Investigate Chrome-iOS-specific behavior and fix it on the same branch/PR:
- CriOS has its own top address bar + bottom toolbar that collapse/expand with scrolling; its WKWebView frame is resized by the app without always firing window resize/visualViewport resize, and dvh/svh/lvh and innerHeight can differ from Safari at first paint (innerHeight may equal the small viewport while the layout viewport later grows, or vice versa).
- Consider: using position: fixed; inset: 0 for the app root (top:0;bottom:0 rather than height), so the root always tracks the real webview bottom even when no event fires; anchoring the build bar with position: fixed; bottom: 0 relative to the viewport; sizing the canvas from the root's getBoundingClientRect via a ResizeObserver on the root/document element (ResizeObserver fires when the fixed root changes size even when window resize doesn't); checking document.documentElement.clientHeight vs innerHeight vs visualViewport.height and taking the max for growth; also listening to 'scroll' on window (CriOS toolbar collapse) and 'focus'/'visibilitychange'. Make sure html/body cannot scroll (overscroll-behavior: none, overflow: hidden) so the toolbar state is stable.
- Prefer the ResizeObserver + fixed inset root approach since it doesn't depend on events.
Emulate with Playwright using a CriOS user agent and simulate a webview that starts short and grows WITHOUT firing resize (e.g. setViewportSize while suppressing events is impossible, so test via a unit test of the layout logic with a mocked ResizeObserver, plus a Playwright check that the root uses fixed inset and the canvas follows ResizeObserver). Keep Safari behavior intact. Run unit tests + lint, rebase only, --force-with-lease. Report what you changed and why it covers Chrome iOS, plus screenshots.
