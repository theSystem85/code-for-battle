UTC: 2026-03-25T14:11:47Z
LLM: GPT-5.3-Codex

Prompt:
Add mobile support for guard mode. ensure if a friendly unit is selected that is a combat unit that then in the mobile action button bar (condensed version only) a new "guard" button appears. This button should just show the SVG icon already used for the guard mode on desktops mouse cursor but on the left of the current repair action button. When the user then clicks on that button the guard mode action button gets in "active" state and gets animated. Before it was not. When active it can be applied to another friendly unit. Once applied the button gets inactive again.
