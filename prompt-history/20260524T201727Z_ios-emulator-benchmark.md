# 2026-05-24T20:17:27Z

LLM: codex

Prompt:

> I added the emulator script to package json. Can you make an e2e performance test that uses the app in the actual ios emulator and there runs the existing game benchmark? So when the test runs it should stat the emulator script and then the e2e test connects to the app in safari to automate it to run the benchmark and then check for the framerate results the benchmark yields and when avg fps below 60fps the test fails?
