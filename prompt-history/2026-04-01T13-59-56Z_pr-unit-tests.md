# PR Unit Tests

**UTC Timestamp:** 2026-04-01T13:59:56Z  
**LLM:** Copilot (GPT-5.4)

## Prompt

> ensure github runs unit tests "npm run unit" when a new commit is pushed to an open PR.

## Follow-up

> there is already a script to run all unit tests called "test:unit" so no need to change package.json for this!

## Summary

- Updated GitHub Actions CI to explicitly include `pull_request.synchronize` so pushes to open PRs rerun CI.
- Kept the existing unit-test command entrypoint as `npm run test:unit`.
- Synced the CI spec and TODO tracking notes with the workflow behavior.